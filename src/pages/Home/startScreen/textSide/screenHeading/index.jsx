import React from 'react';
import classes from './styles.module.scss';

const ScreenHeading = () => {
    return (
        <h2 className={classes.heading}>The only blockchain<br/>the world will ever need</h2>
    );
};

export default ScreenHeading;